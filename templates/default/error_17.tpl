{* Undefined attribute type error -> invalid schema }
<span class="error">
LDAP Error #{$errno}: {$error}<p>
This error is caused by an invalid schema. Have you updated them 
recently? If not, copy the new schema that defines this attribute, and 
restart your LDAP server.
</span>
