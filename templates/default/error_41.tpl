{* Object Class Violation error }
<span class="error">
LDAP Error #{$errno}: {$error}<p>
This error is caused because you're using an attribute but haven't
included the correct object class.This error is caused because you're
using an attribute but haven't included the correct object class.
</span>
